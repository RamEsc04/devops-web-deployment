apiVersion: v1
kind: Service
metadata:
  name: devops-web-service
  namespace: web-dev
  labels:
    environment: dev
spec:
  type: NodePort
  selector:
    app: devops-web
    environment: dev
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30081
