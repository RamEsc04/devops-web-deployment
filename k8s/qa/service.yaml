apiVersion: v1
kind: Service
metadata:
  name: devops-web-service
  namespace: web-qa
  labels:
    environment: qa
spec:
  type: NodePort
  selector:
    app: devops-web
    environment: qa
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30082
